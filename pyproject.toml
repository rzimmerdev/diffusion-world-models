[project]
name = "diffusion-world-models"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "marimo==0.23.8",
    "bibtexparser",
]

[tool.marimo.runtime]
output_max_bytes = 20_000_000
